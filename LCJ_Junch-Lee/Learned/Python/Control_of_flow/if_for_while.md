## 1. if 문

조건문의 기본 형태

if 문에 작성된 조건을 만족할 때 내부 코드 실행

elif : 추가적인 다른 조건

else : 모든 조건 만족 X 
- 경우에 따라 `if-elif`만을 사용해도 무관함.

### 중첩 조건문 : 조건문 내부에 또 다른 조건문 작성 가능 


## **반복문** (중요)

: 주어진 코드 블록을 여러 번 반복해서 실행하는 구문 


### 2. for 문

```python
for 변수 in 반복가능 객체

# 숫자형 (int, float) 은 반복가능 객체가 X!
# range()를 활용
```
: 반복 가능(iterable)한 객체의 요소들을 반복하는데 주로 사용 

- 반복 가능한 객체 요소의 개수만큼 반복 
- 특징 : 반복 횟수가 정해져 있음
- 변수 : for문 내에서 활용되는 임시변수 

### 3. while 문

: 조건 참(True) 인 동안 반복 
: 조건식이 거짓(False)이 될 때 까지 반복 실행

- 반복 횟수가 정해지지 않은 경우 사용 
  > 언제까지 수행할 것인가가 포인트 


#### 딕셔너리는 Key를 추출해낸다. 

- 반복 순서는 보장 됨! (딕셔너리 추출 시 순서는 보장되기 때문) 

## 중첩된 반복문

리스트 내의 리스트 요소들을 참조해야 할 때. 


## 반복 제어

for문과 while은 매 반복마다 본문 내 모든 코드를 실행하지만 때때로 일부만 실행하는 것이 필요할 때가 있음 

### break

: 해당 키워드를 만나게 되면 남은 코드를 무시하고 반복 즉시 종료

- 반복을 끝내야 할 명확한 조건이 있을 때 사용 

```python

for i in range(10):
    if i == 5:
        break

    print(i) # 0 1 2 3 4
```


### continue

: 해당 키워드를 만나게 되면 다음 코드는 무시하고 다음 반복을 수행

```python

for i in range(10):
    if i % 2 == 0:
        continue
    
    print(i) # 1 3 5 7 9
```

### flag 변수 

: 상태 확인, 특정한 조건을 만족시킬 경우 상태를 뒤집어 버림.

`found_even = False`과 같은 변수 


## pass 

: 빈 코드 블록 키워드 

- 아무 동작도 하지 않음을 명시적으로 나타내는 키워드
- 반복 제어가 아닌 '코드의 틀을 유지'하거나 나중에 내용을 채우기 위한 용도 '

## 심화 : 유용한 내장 함수 map, zip 


반복 가능한 데이터구조의 모든 요소에 function을 적용하고, 그 결과 값들을 map object로 묶어서 반환. 

### zip 

여러 개의 반복 가능한 데이터 구조를 묶어서, 같은 위치(인덱스)에 있는 값들을 하나의 tuple로 만든 뒤 그것들을 모아 zip object로 반환