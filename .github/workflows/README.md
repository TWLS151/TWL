# GitHub Actions - Daily Issue System

> GitHub Actions를 사용하여 매일 자동으로 TIL(Today I Learned) 이슈를 생성하고 종료하는 시스템

## 📋 System Overview

이 시스템은 두 개의 자동화된 워크플로우로 구성되어 있습니다:

### 1️⃣ Daily Issue Open (`daily-issue-open.yml`)
- **실행 시간**: 매일 21:00 (UTC) → **오전 6:00 (KST)**
- **역할**: 새로운 TIL 이슈 자동 생성
- **자동 레이블**: `daily-til`, `auto-generated`

### 2️⃣ Daily Issue Close (`daily-issue-close.yml`)
- **실행 시간**: 매일 14:00 (UTC) → **밤 11:00 (KST)**
- **역할**: 당일 TIL 이슈 자동 종료 및 완료 댓글 추가

## 🚀 Getting Started

### 필수 요구사항
- GitHub 저장소 (Public 또는 Private)
- GitHub Actions 활성화 (대부분의 저장소에서 기본 활성화)

### 1. 저장소 설정 확인
```
Settings → Actions → General
```
**"Allow all actions and reusable workflows"** 또는 **"Allow GitHub Actions to create and approve pull requests"** 선택

### 2. Workflow 권한 설정
```
Settings → Actions → General → Workflow permissions
```
- ✅ **Read repository contents** 
- ✅ **Write issues**

### 3. 시간대 커스터마이징 (선택사항)

원하는 시간에 실행되도록 변경하려면:

**`.github/workflows/daily-issue-open.yml`** 파일에서:
```yaml
- cron: '0 21 * * *'  # 현재: 매일 21:00 (UTC) = 06:00 (KST)
```

**`.github/workflows/daily-issue-close.yml`** 파일에서:
```yaml
- cron: '0 14 * * *'  # 현재: 매일 14:00 (UTC) = 23:00 (KST)
```

#### Cron 표현식 가이드
```
┌───────────── 분 (0 - 59)
│ ┌───────────── 시간 (0 - 23)
│ │ ┌───────────── 일 (1 - 31)
│ │ │ ┌───────────── 월 (1 - 12)
│ │ │ │ ┌───────────── 요일 (0 - 6) (0 = 일요일)
│ │ │ │ │
│ │ │ │ │
* * * * *
```

**예시:**
- `0 9 * * *` → 매일 09:00
- `0 0 * * *` → 매일 00:00 (자정)
- `30 14 * * 1` → 매주 월요일 14:30
- `0 9 * * 1-5` → 평일(월-금) 매일 09:00

### 4. 수동 트리거 (선택사항)

GitHub UI에서 수동으로 워크플로우를 실행할 수 있습니다:
1. **Actions** 탭 이동
2. 워크플로우 선택 (예: "Daily Issue Open")
3. **Run workflow** 클릭

## 📝 Daily Issue Template

자동 생성되는 이슈의 기본 템플릿:

```
## 📅 Today's Learning

Date: YYYY-MM-DD

### ✍️ What I learned today:
- [ ] 학습 내용 1
- [ ] 학습 내용 2
- [ ] 학습 내용 3

### 📚 Resources:
- 자료 링크 1
- 자료 링크 2

### 🎯 Tomorrow's Goal:
- 내일 공부할 내용 작성

### 💡 Notes:
자유롭게 메모 작성
```

## 🔍 Issue 검색 및 필터링

### GitHub Issues에서 검색
```
label:daily-til                    # 모든 일일 이슈 조회
label:daily-til is:closed          # 종료된 일일 이슈 조회
label:daily-til is:open            # 열려있는 일일 이슈 조회
created:2026-01-26                 # 특정 날짜 이슈 조회
```

## 🛠️ 워크플로우 커스터마이징

### 이슈 제목 변경
`.github/workflows/daily-issue-open.yml` 수정:
```yaml
const title = `📝 Daily TIL - ${today}`;  # 이 부분 변경
```

### 이슈 본문 템플릿 변경
`.github/workflows/daily-issue-open.yml` 수정:
```yaml
const body = `
# 여기에 원하는 템플릿 작성
`;
```

### 자동 레이블 추가
`.github/workflows/daily-issue-open.yml` 수정:
```yaml
labels: ['daily-til', 'auto-generated', 'custom-label']
```

## 📊 워크플로우 모니터링

### 실행 로그 확인
1. **Actions** 탭 이동
2. 워크플로우 선택
3. 최근 실행 클릭
4. 상세 로그 확인

### 실패 시 트러블슈팅

**Issue 1: 이슈가 생성되지 않음**
- ✅ Workflow permissions 확인
- ✅ Cron 시간대 확인 (UTC 기준)
- ✅ GitHub Actions 활성화 확인

**Issue 2: 자동 종료 실패**
- ✅ 이슈 레이블 확인 (`daily-til` 필수)
- ✅ 날짜 형식 일치 확인

## 🎯 활용 팁

### 1. 매일 시작하기
GitHub Issues 페이블에서 자동 생성된 Daily TIL 이슈 확인

### 2. 학습 내용 기록
체크박스를 클릭하며 오늘의 학습 내용 정리

### 3. 자동 종료 및 아카이브
매일 밤 워크플로우가 자동으로 이슈 종료 및 완료 댓글 추가

### 4. 학습 히스토리 추적
라벨 필터링으로 과거 학습 기록 쉽게 조회

## 🔐 보안 고려사항

- ✅ 워크플로우는 저장소의 `${{ secrets.GITHUB_TOKEN }}`을 사용
- ✅ 권한은 자동으로 제한됨 (이슈 읽기/쓰기만 가능)
- ✅ Personal Access Token 불필요

## 📚 참고 문서

- [GitHub Actions 공식 문서](https://docs.github.com/en/actions)
- [GitHub Script 사용 가이드](https://github.com/actions/github-script)
- [Cron 표현식 온라인 테스터](https://crontab.guru/)

---

**마지막 수정**: 2026-01-30  
**Author**: TWL Team  
**Status**: Active ✅
