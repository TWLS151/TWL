# 백준 1268 임시반장을 풀었습니다.

이제 슬슬 등급을 올려야겠다 싶어서 올렸습니다.
빠져서 브론즈만 풀다가는 안되겠다 싶어서 실버5로 소심하게 올렸는데요?
1시간 동안 집중 못하고 있다는게 느껴져서 상태의 심각성을 느끼고 이제 빡 집중 코어 타임을 만들기로 했습니다. 더 나아가서 집중력 향상 키트를 구비하기로 마음 먹었습니다.
별건 없고 그냥 콜라, 얼음, 졸음껌 세트 입니다.

후후 이거 갖춰진 한영욱 이제 못 막을 겁니다.


아니 얼마나 요즘 감이 떨어졌으면 이것도 눈치를 못채는지....

그래도 하루걸러 하루 거만함 억제기를 만나는 것 같아 좋습니다.

암튼!
그래서 오늘의 풀이를 알려드립니다.


```python

N = int(input())
SL = []
for i in range(N):
    SL.append(list(map(int, input().split())))


# print(SL)
#### 원래 사고 흐름 ---
##  이걸 어떻게 저장해야 좋을까
# 1. 반 별로 인원을 저장
# 2. 번호별 학생 저장
# 3.  MAX로 뽑고 인덱스 +1 출력하면 될듯.
## 답 사고 흐름 => 여러번 같은 반은 세면 안되어서 안됨
# 같은 애들만 카운트하고 넘어감
cnt = [0] * N

for i in range(N):          # 기준 학생
    for k in range(N):      # 비교 학생
        if i == k:
            continue
        for g in range(5):  # 학년
            if SL[i][g] == SL[k][g]:
                cnt[i] += 1
                break       # 한 번이라도 같으면 끝

print(cnt.index(max(cnt)) + 1)


```

보시면 아시겠지만 문제 개념을 아예 잘 못 이해하고 있었고, 그걸 해결하고 푸는게 오래걸렸습니다.


이래서 언제 DP감각 다시 쌓고, 언제 BFS,DFS감각 다시 쌓나 싶습니다.

그치만 천천히다보면 쌓이겠죠 뭐 


이제 곧 정처기 시험이라 정신차리고, 공부하느냐고 당분간은 그거만 올리고자 합니다.


응원 부탁드립니다 입니다. 응원 해주신다니까 감사티비.